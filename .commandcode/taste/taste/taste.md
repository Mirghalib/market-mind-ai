# Taste
- Prefers comprehensive, phased, systematic end-to-end audits of the entire project before making any changes — inspect everything, report every issue, then fix. Confidence: 0.8
- Strongly prefers preserving existing architecture and structure: do NOT regenerate the project, do NOT rewrite architecture, do NOT remove working code — only fix broken code. Confidence: 0.9
- Wants session-continuous execution: resume exactly from the last completed implementation, never restart the project or files, never overwrite working code — first verify the build still compiles, then continue the remaining checklist from where the previous session stopped. Confidence: 0.9
- Wants every fix explained, then the project re-run/verified after each fix, continuing automatically until the whole app works end-to-end (backend starts, frontend starts, frontend talks to backend, DB/AI/auth/RBAC/uploads/export/history/admin all work, no console/network/runtime errors, no 500s). Confidence: 0.8
- Expects full-stack coverage in a single engagement — frontend, backend, database, DevOps, security, QA, and product/UX all reviewed together (multi-role expert framing). Confidence: 0.7
- Mock/fallback degradation for external dependencies (e.g. AI provider rate-limited or unavailable) is acceptable only as an explicit opt-in via config (AI_FALLBACK_TO_MOCK=true) for offline demos/CI. When a real API/model is configured and fails, never silently substitute a deterministic dummy strategy — return an error (502/503) instead of generating a document, and never persist fake output as a SUCCESS result. Confidence: 0.9
- Wants a professional final report with a health score per area (project, backend, frontend, security, database, performance, UI/UX, AI pipeline), a list of bugs found, fixes applied, remaining improvements, and a verified-features checklist with PASS/FAIL status. Confidence: 0.8
- Expects the final implementation report to follow a specific numbered structure: files changed, database migrations, new dependencies, environment variables required, API endpoints added, features implemented, bugs fixed, verification performed, remaining issues. Confidence: 0.8
- Demands a production-grade, commercial-SaaS polish bar for the final deliverable: no placeholders, no mock data, no broken or inactive buttons, no incomplete features — everything wired to real backend/AI data and demo-ready as a "hackathon-winning" product, not a prototype. Confidence: 0.95
- Expects AI-generated content (e.g. marketing strategies) to read like a professional consultant wrote it — unique per generation, no generic templates or placeholder text, with a comprehensive, structured section set (executive summary through action plans). Confidence: 0.7
- For explicit multi-phase execution orders (a stated mission with phases and a final-report requirement), prefers the agent to proceed directly with implementation using a tracked todo list rather than pausing to ask for a plan. Confidence: 0.5
- When a deliberate behavior change (e.g. an endpoint now requiring auth, or a format becoming supported) breaks existing tests, expects the tests to be updated to assert the new intended behavior — not to revert the change. Confidence: 0.8
- When implementing a feature, prefers to clarify consequential design decisions upfront (via targeted questions with recommended options) before writing the plan, then proceeds with the confirmed choices. Confidence: 0.7
- Expects a designed, stakeholder-presentable artifact for any user-facing deliverable: cover page with logo, professional typography, corporate color palette, headers/footers and page numbers, visual elements (tables, charts, progress bars, summary cards), and every reported metric tied to a real, computed source. Confidence: 0.8
- Prefers feature rollouts to be fully end-to-end verified by the implementer, not just delivered: run the backend test suite, build the frontend, exercise each new action (including loading and error states), and confirm generated files actually open/work. Confidence: 0.8
- Explicitly rejects temporary fixes or band-aids: bug reports must be resolved by finding the true root cause (trace the full chain — refs, event binding, overlays/z-index, data flow, API responses, DB persistence, static serving) and fixing it completely rather than patching symptoms. Confidence: 0.9
- Expects user identity/profile changes (e.g. profile image) to propagate immediately to every UI surface (navbar, sidebar, profile page, admin panel, user lists) and to persist across logout/login, refresh, and browser restart — via re-hydrating the user profile from the backend on mount, not stale local storage; initials placeholders must appear only when no avatar exists. Confidence: 0.9
- Prefers real profile photographs over initials/character placeholders for avatars in user-facing social-proof UI (e.g. "Loved by marketing teams" testimonial cards) — initials like "JD" should be replaced with actual people photos. Confidence: 0.6
- Expects stored file paths (e.g. uploaded profile images) to be exposed as complete, publicly reachable URLs consistently across ALL backend endpoints (auth/me, register, admin users, analytics) — never raw relative paths that 404 in the browser. Confidence: 0.8
- For bug-fix work, wants the final report to lead with the root cause of each bug, followed by files modified, APIs modified, components modified, backend changes, frontend changes, then verification results. Confidence: 0.7
- Prefers top-level CTA buttons that scroll to a section to be pure navigation — they must NEVER directly call the backend API; only the form's own submit button triggers generation. Confidence: 0.8
- Expects scroll-to-form CTAs to end with the first field auto-focused (smooth scroll → section → focus first input). Confidence: 0.6
- Expects admin interfaces to include full management control over all users' data (not just read-only analytics): list every user's exports with search/filter/sort/pagination, download, view owner/strategy details, and permanently delete records (DB row + file). Confidence: 0.6
- Maintains responsive design as a hard requirement: layouts must work and be explicitly verified on desktop, tablet, and mobile. Confidence: 0.7
- Expects auth pages (login/signup) to always provide a visible "back to home" navigation affordance so users can return to the landing page without using the browser back button. Confidence: 0.5
- Prefers symmetric, equal-height card grids on dashboards: all cards in a row stretch to the tallest card's height and content fills the card, so short-content cards don't leave large empty gaps. Long content is truncated with a "Read more" affordance, and expanding one card must NEVER cause its row-mates to grow/resize — full content should open as an overlay (modal) rather than inline expansion, so the grid height never reflows. Confidence: 0.9
- Prefers role-differentiated dashboards — admins get platform analytics and must never have business-user features (e.g. strategy generation); user dashboards stay business workspaces; no duplicated layouts across roles. Confidence: 0.7
- Likes polished, animated analytics widgets for dashboards: icon, animated count-up, growth indicator, tooltip, and hover/entrance animations. Confidence: 0.5
- Expects configuration and secrets (e.g., SMTP credentials, sender accounts) to be driven by environment variables (.env), never hardcoded in code — and for newly added .env variables to be documented so they can be supplied at deploy time. New AI providers get a provider-named API key variable in .env, exactly as the user names it (e.g. GEMINI_API_KEY), and provider settings such as base_url/model/api_key are explicitly requested to be .env-configurable. Confidence: 0.9
- Wants global, token-driven theming: every page (landing, auth, dashboard, admin, history, strategy views) must respond instantly to Light/Dark/custom theme changes through a shared theme provider, with hardcoded color classes eliminated everywhere. Confidence: 0.8
- Prefers additive, non-breaking change when extending a system: existing API contracts and functionality must be preserved ("Do NOT change existing APIs unless necessary"); new behavior is added via new endpoints/params rather than altering existing ones. Confidence: 0.8
- Requires one consistent brand system across every user-facing deliverable and export format — shared fonts, corporate palette (purple), modern spacing, icons, tables/charts, and clear section hierarchy — so all outputs look client-ready and competition-grade (benchmarked to top-tier consultancy quality). Confidence: 0.7
- Prefers branded HTML emails (logo header, personal greeting, summary/score, CTA button, footer) over plain-text messages; HTML is the default unless unavailable. Confidence: 0.6
- When switching AI providers (or config options generally), prefers the old provider's settings to be commented out rather than deleted, so switching back is a trivial uncomment — explicitly: "donot remove groq just commond it". Confidence: 0.9
- When the active AI provider exhausts its quota (free-tier 429 rate limits), prefers switching to an already-configured alternative provider (e.g. Groq) so generation keeps working rather than waiting for the quota to reset or retrying the exhausted provider. Confidence: 0.5
- Prefers AI model/provider selection to be controllable from the frontend admin panel at runtime (e.g. Groq/llama vs Infron/qwen), not only via .env edits. Confidence: 0.8
- Prefers the OpenAI SDK as the single LLM interface: any OpenAI-compatible provider is configured purely via .env (single AI_BASE_URL + AI_API_KEY + AI_MODEL), and multi-provider adapter classes (Groq, Anthropic, Gemini, etc.) are explicitly removed rather than maintained, so switching providers is a config change, not a code change. Confidence: 0.9
tates), and confirm generated files actually open/work. Confidence: 0.8
roduct, country, budget, audience, competitors, goal): different industries/countries/budgets must yield different channels, ad platforms, content types, KPIs, SEO, and ROI — never one template with only the business name swapped in. Confidence: 0.9
- No hardcoded currency/USD assumptions: the app must be country- and currency-aware, with the currency auto-selected from the country (manual override allowed) and every monetary figure (budget, ad spend, ROI) rendered in that currency across the form, the AI output, and all exports. Confidence: 0.9
- Marketing recommendations must be localized by country AND budget (e.g., Pakistan → Facebook/TikTok/WhatsApp/Daraz, USA → LinkedIn/HubSpot/Google/Email automation, UAE → Instagram/Arabic campaigns/luxury branding/influencers). Confidence: 0.8
- Consulting-grade export QA: exports must never show overlapping or cut-off text/charts/tables, broken layouts, or missing page numbers — clean, printable pages benchmarked to top-tier consulting (McKinsey/Deloitte/BCG) quality. The user personally inspects every rendered format (PDF/DOCX/PPTX) and reports even single visual defects (blank pages, text overwrites, chart elements colliding with rules/lines), so spacing/clearance between elements is a hard requirement. Confidence: 0.95
- Modals must never overflow the viewport on large content: cap the panel height (e.g. max-h-[85vh]) with a pinned header, and give the body its own internal scrollbar (min-h-0 flex-1 overflow-y-auto) — long content scrolls inside the modal instead of growing past the screen. Confidence: 0.7
- Expects personalization to be proven across a representative set of industries (restaurant, SaaS, gym, hospital, academy, travel agency, beauty salon, furniture, real estate, ecommerce) and the final report to include evidence (live API output / screenshots) that different businesses get different strategies and that every export renders cleanly. Confidence: 0.7
tates), and confirm generated files actually open/work. Confidence: 0.8
ted API using the official vendor SDK. Confidence: 0.5
tates), and confirm generated files actually open/work. Confidence: 0.8

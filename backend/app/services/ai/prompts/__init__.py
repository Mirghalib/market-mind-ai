"""Prompt templates for the AI pipeline.

One template per marketing deliverable. Templates define the input
contract (what context the PromptBuilder must provide) and the output
contract (the JSON shape the model must return). No LLM logic here.
"""

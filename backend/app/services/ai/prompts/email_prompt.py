"""email_prompt.py — email marketing copy generation.

Input contract (context keys):
    brand, product, audience, email_type, subject_hint, goal

Output contract:
    JSON with subject lines, preheader, body sections, and CTA.
    Shape defined in app.services.ai.models.
"""

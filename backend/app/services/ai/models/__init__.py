"""Typed output models for the AI pipeline.

These describe the structured, validated shape of what each prompt
deliverable returns — the contract AIService guarantees to the app.
Distinct from app/models (SQLAlchemy) and app/schemas (API layer).
"""

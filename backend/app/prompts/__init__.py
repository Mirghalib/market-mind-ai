"""LLM prompt templates.

Versionable, testable prompt modules for the AI features. Each prompt
defines the system/user instructions for a single use case, e.g.:

    prompts/
    ├── market_analysis.py   #  system + user prompt builders
    └── __init__.py
"""

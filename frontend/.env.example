# Frontend environment configuration (copy to .env to override).
# Defaults to the local backend; change for deployed environments.

# Base URL of the FastAPI backend (includes the /api/v1 version prefix).
VITE_API_BASE_URL=http://localhost:8000/api/v1
